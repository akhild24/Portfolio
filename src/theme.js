/* ── Design System Tokens ─────────────────────────────────────────────────────
   Single source of truth for the Operational Editorial palette.
   All values reference CSS custom properties declared in index.css @theme.
   ──────────────────────────────────────────────────────────────────────────── */

/* ── Surfaces (void → light) ── */
export const VOID     = "var(--color-void)";
export const SURFACE  = "var(--color-surface)";
export const PLATE    = "var(--color-plate)";
export const SMOKE    = "var(--color-smoke)";
export const GRAPHITE = "var(--color-graphite)";

/* ── Inverted surface ── */
export const LINEN = "var(--color-linen)";

/* ── Text ── */
export const BONE = "var(--color-bone)";
export const ASH  = "var(--color-ash)";
export const DIM  = "var(--color-dim)";
export const IRON = "var(--color-iron)";
export const INK  = "var(--color-ink)";

/* ── Accents ── */
export const SIGNAL   = "var(--color-signal)";
export const ARTERIAL = "#fe1e34";

/* ── Fonts — three voices ── */
export const MONO  = "var(--font-mono)";
export const SANS  = "var(--font-sans)";
export const SERIF = "var(--font-serif)";
